{
    "rules": {
      "no-underscore-dangle": ["error", {"allow":["_id"]}],
      "linebreak-style": ["error", "unix"], //Вынужденный фикс - ругался на разрыв строк, пришлось поставить такое правило, но я пишу на винде, если подскажите почему так, то буду очень благодарен
      "no-console": ["error", { "allow": ["log"] }],
      "no-unused-vars": ["error", { "vars": "all", "args": "after-used","varsIgnorePattern": "password","ignoreRestSiblings": false }]
    },
    "extends": "airbnb-base"
  }